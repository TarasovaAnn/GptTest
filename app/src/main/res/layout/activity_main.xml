<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity">

    <!-- Ваш Welcome (center, исчезает после 1 msg) -->
    <TextView
        android:id="@+id/welcome_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:gravity="center"
        android:padding="32dp"
        android:text="Welcome to ChatGPT\nTry it out now"
        android:textSize="28dp"
        android:textStyle="bold" />

    <!-- RecyclerView (сжимается под клавой, ваш padding=8dp) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        tools:listitem="@layout/chat_item" />  <!-- Ваш RelativeLayout с bubbles! -->

    <!-- ВАШ bottom_layout (80dp, rounded, кнопка СПРАВА/ЦЕНТР) -->
    <RelativeLayout
        android:id="@+id/bottom_layout"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackground"
        android:elevation="4dp">

        <!-- EditText (ваш стиль: rounded, centerVertical, toLeftOf кнопки) -->
        <EditText
            android:id="@+id/message_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@id/send_btn"
            android:layout_marginEnd="6dp"
            android:background="@drawable/rounded_corner"
            android:hint="Write here"
            android:inputType="textMultiLine|textCapSentences"
            android:imeOptions="actionSend|flagNoExtractUi"
            android:padding="16dp"
            android:textColor="@color/black"
            android:textSize="16sp" />

        <!-- Кнопка (СПРАВА, centerVertical, БЕЗ marginStart!) -->
        <ImageButton
            android:id="@+id/send_btn"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Send"
            android:padding="8dp"
            android:src="@android:drawable/ic_menu_send" />

    </RelativeLayout>

</LinearLayout>